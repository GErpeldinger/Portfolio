{% extends('admin/base.html.twig') %}

{% block title %}{{ 'nouveau' in app.request.pathInfo ? 'Nouveau' : 'Editer' }} projet{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center align-items-center vh-100">
        <div class="col-12">
            <h2 class="text-center">{{ 'nouveau' in app.request.pathInfo ? 'Nouveau' : 'Editer' }} projet</h2>
            {{ form_start(form) }}
                {{ form_row(form.title) }}
                {{ form_row(form.description) }}
                {{ form_row(form.imageFile) }}
                {{ form_row(form.url) }}
                <div class="row justify-content-around">
                    {{ form_row(form.isVideo) }}
                    {{ form_row(form.isWebsite) }}
                </div>
                <div class="d-flex justify-content-end">
                    <a class="btn btn-wild" href="{{ path('project_list') }}">Retour</a>
                    <button class="btn btn-wild ml-2">Ajouter</button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
